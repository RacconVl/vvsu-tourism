openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: API for VVSU Tourism & Creative Industries immersive educational platform
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: courses
    description: Course and module operations
  - name: quests
    description: Quest and task operations
  - name: achievements
    description: Achievements and badges
  - name: map
    description: Interactive Vladivostok map points
  - name: community
    description: Forum and student works
  - name: library
    description: Educational resources
  - name: dashboard
    description: Student dashboard and progress
  - name: quizzes
    description: Interactive knowledge quizzes about Vladivostok and Primorsky Krai
  - name: auth
    description: Authentication and session management
  - name: profile
    description: Personal profile and statistics
  - name: admin
    description: Admin-only operations

paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  /courses:
    get:
      operationId: listCourses
      tags: [courses]
      summary: List all courses
      responses:
        "200":
          description: List of courses
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Course"

  /courses/{id}:
    get:
      operationId: getCourse
      tags: [courses]
      summary: Get course with modules
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Course detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CourseDetail"

  /modules/{id}/complete:
    post:
      operationId: completeModule
      tags: [courses]
      summary: Mark a module as complete
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Module completion result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ModuleCompletion"

  /quests:
    get:
      operationId: listQuests
      tags: [quests]
      summary: List all quests
      responses:
        "200":
          description: List of quests
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Quest"

  /quests/{id}:
    get:
      operationId: getQuest
      tags: [quests]
      summary: Get quest detail
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Quest detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Quest"

  /quests/{id}/submit:
    post:
      operationId: submitQuest
      tags: [quests]
      summary: Submit quest answer
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QuestSubmission"
      responses:
        "200":
          description: Submission result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuestResult"

  /achievements:
    get:
      operationId: listAchievements
      tags: [achievements]
      summary: List all achievements with unlock status
      responses:
        "200":
          description: Achievements list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Achievement"

  /map/points:
    get:
      operationId: listMapPoints
      tags: [map]
      summary: List all interactive map points
      responses:
        "200":
          description: Map points
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/MapPoint"

  /map/routes:
    get:
      operationId: listMapRoutes
      tags: [map]
      summary: List tourist routes
      responses:
        "200":
          description: Tourist routes
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TouristRoute"

  /community/posts:
    get:
      operationId: listCommunityPosts
      tags: [community]
      summary: List community forum posts
      responses:
        "200":
          description: Community posts
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CommunityPost"
    post:
      operationId: createCommunityPost
      tags: [community]
      summary: Create a community post
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateCommunityPostBody"
      responses:
        "201":
          description: Created post
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CommunityPost"

  /community/gallery:
    get:
      operationId: listGalleryWorks
      tags: [community]
      summary: List student gallery works
      responses:
        "200":
          description: Gallery works
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/GalleryWork"

  /library/resources:
    get:
      operationId: listLibraryResources
      tags: [library]
      summary: List educational library resources
      responses:
        "200":
          description: Library resources
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LibraryResource"

  /dashboard/summary:
    get:
      operationId: getDashboardSummary
      tags: [dashboard]
      summary: Get student dashboard summary
      responses:
        "200":
          description: Dashboard summary with progress and stats
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardSummary"

  /dashboard/progress:
    get:
      operationId: getProgressMap
      tags: [dashboard]
      summary: Get travel map progress visualization data
      responses:
        "200":
          description: Progress map data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProgressMap"

  /auth/register:
    post:
      operationId: register
      tags: [auth]
      summary: Register a new account
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterRequest"
      responses:
        "200":
          description: Registered user with JWT token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthWithToken"

  /auth/login:
    post:
      operationId: login
      tags: [auth]
      summary: Sign in
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginRequest"
      responses:
        "200":
          description: Logged in user with JWT token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthWithToken"

  /auth/logout:
    post:
      operationId: logout
      tags: [auth]
      summary: Sign out
      responses:
        "200":
          description: Logged out
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OkResponse"

  /auth/me:
    get:
      operationId: getMe
      tags: [auth]
      summary: Get current authenticated user
      responses:
        "200":
          description: Current user or null
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeResponse"

  /profile:
    get:
      operationId: getMyProfile
      tags: [profile]
      summary: Get my full profile with stats
      responses:
        "200":
          description: Profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Profile"
    patch:
      operationId: updateMyProfile
      tags: [profile]
      summary: Update my profile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateProfileRequest"
      responses:
        "200":
          description: Updated user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthUser"

  /profile/{id}:
    get:
      operationId: getPublicProfile
      tags: [profile]
      summary: Get public profile of a user
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Public profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicProfile"

  /admin/users:
    get:
      operationId: adminListUsers
      tags: [admin]
      summary: List all users (admin only)
      responses:
        "200":
          description: User list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AdminUserRow"

  /admin/stats:
    get:
      operationId: adminGetStats
      tags: [admin]
      summary: Aggregate platform statistics
      responses:
        "200":
          description: Stats
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminStats"

  /admin/courses:
    post:
      operationId: adminCreateCourse
      tags: [admin]
      summary: Create a new course
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateCourseRequest"
      responses:
        "200":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Course"

  /admin/quests:
    post:
      operationId: adminCreateQuest
      tags: [admin]
      summary: Create a new quest
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateQuestRequest"
      responses:
        "200":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Quest"

  /quizzes:
    get:
      operationId: listQuizzes
      tags: [quizzes]
      summary: List all quizzes
      responses:
        "200":
          description: List of quizzes
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/QuizSummary"

  /quizzes/{id}:
    get:
      operationId: getQuiz
      tags: [quizzes]
      summary: Get full quiz with questions
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Quiz with questions
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Quiz"

  /quizzes/{id}/submit:
    post:
      operationId: submitQuiz
      tags: [quizzes]
      summary: Submit quiz answers and get results
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QuizSubmission"
      responses:
        "200":
          description: Quiz result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuizResult"

  /dashboard/leaderboard:
    get:
      operationId: getLeaderboard
      tags: [dashboard]
      summary: Get top students leaderboard
      responses:
        "200":
          description: Leaderboard
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LeaderboardEntry"

components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required: [status]

    Course:
      type: object
      properties:
        id:
          type: integer
        title:
          type: string
        description:
          type: string
        role:
          type: string
          description: "Student role: guide, marketer, designer, operator"
        stage:
          type: string
          description: "Journey stage name (e.g. Порт отправления)"
        imageUrl:
          type: string
        totalModules:
          type: integer
        completedModules:
          type: integer
        xpReward:
          type: integer
        isLocked:
          type: boolean
        category:
          type: string
      required: [id, title, description, role, stage, totalModules, completedModules, xpReward, isLocked, category]

    Module:
      type: object
      properties:
        id:
          type: integer
        courseId:
          type: integer
        title:
          type: string
        type:
          type: string
          description: "video, interactive, animation, test"
        order:
          type: integer
        isCompleted:
          type: boolean
        durationMinutes:
          type: integer
        xpReward:
          type: integer
      required: [id, courseId, title, type, order, isCompleted, durationMinutes, xpReward]

    CourseDetail:
      allOf:
        - $ref: "#/components/schemas/Course"
        - type: object
          properties:
            modules:
              type: array
              items:
                $ref: "#/components/schemas/Module"
          required: [modules]

    ModuleCompletion:
      type: object
      properties:
        success:
          type: boolean
        xpEarned:
          type: integer
        newAchievements:
          type: array
          items:
            $ref: "#/components/schemas/Achievement"
      required: [success, xpEarned, newAchievements]

    Quest:
      type: object
      properties:
        id:
          type: integer
        title:
          type: string
        description:
          type: string
        type:
          type: string
          description: "route, budget, marketing, design"
        difficulty:
          type: string
          description: "easy, medium, hard"
        xpReward:
          type: integer
        isCompleted:
          type: boolean
        imageUrl:
          type: string
        locationName:
          type: string
      required: [id, title, description, type, difficulty, xpReward, isCompleted, locationName]

    QuestSubmission:
      type: object
      properties:
        answer:
          type: string
      required: [answer]

    QuestResult:
      type: object
      properties:
        success:
          type: boolean
        xpEarned:
          type: integer
        feedback:
          type: string
        newAchievements:
          type: array
          items:
            $ref: "#/components/schemas/Achievement"
      required: [success, xpEarned, feedback, newAchievements]

    Achievement:
      type: object
      properties:
        id:
          type: integer
        title:
          type: string
        description:
          type: string
        icon:
          type: string
        category:
          type: string
          description: "exploration, learning, social, mastery"
        isUnlocked:
          type: boolean
        unlockedAt:
          type: string
          nullable: true
        xpValue:
          type: integer
      required: [id, title, description, icon, category, isUnlocked, xpValue]

    MapPoint:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        description:
          type: string
        category:
          type: string
          description: "landmark, hotel, museum, nature, restaurant"
        lat:
          type: number
        lng:
          type: number
        imageUrl:
          type: string
        videoUrl:
          type: string
          nullable: true
        relatedCourseId:
          type: integer
          nullable: true
        legend:
          type: string
          nullable: true
          description: "Local folk legend or historical story"
      required: [id, name, description, category, lat, lng, imageUrl]

    TouristRoute:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        description:
          type: string
        durationHours:
          type: number
        pointIds:
          type: array
          items:
            type: integer
        difficulty:
          type: string
        authorName:
          type: string
        isStudentCreated:
          type: boolean
      required: [id, name, description, durationHours, pointIds, difficulty, authorName, isStudentCreated]

    CommunityPost:
      type: object
      properties:
        id:
          type: integer
        title:
          type: string
        content:
          type: string
        authorName:
          type: string
        authorRole:
          type: string
        createdAt:
          type: string
        likes:
          type: integer
        replies:
          type: integer
        category:
          type: string
      required: [id, title, content, authorName, authorRole, createdAt, likes, replies, category]

    CreateCommunityPostBody:
      type: object
      properties:
        title:
          type: string
        content:
          type: string
        category:
          type: string
      required: [title, content, category]

    GalleryWork:
      type: object
      properties:
        id:
          type: integer
        title:
          type: string
        description:
          type: string
        authorName:
          type: string
        category:
          type: string
          description: "design, marketing, route, concept"
        imageUrl:
          type: string
        likes:
          type: integer
        createdAt:
          type: string
      required: [id, title, description, authorName, category, imageUrl, likes, createdAt]

    LibraryResource:
      type: object
      properties:
        id:
          type: integer
        title:
          type: string
        description:
          type: string
        type:
          type: string
          description: "presentation, infographic, video, article, database"
        category:
          type: string
        url:
          type: string
        thumbnailUrl:
          type: string
        isInteractive:
          type: boolean
      required: [id, title, description, type, category, url, thumbnailUrl, isInteractive]

    DashboardSummary:
      type: object
      properties:
        studentName:
          type: string
        currentRole:
          type: string
        level:
          type: integer
        xp:
          type: integer
        xpToNextLevel:
          type: integer
        currentStage:
          type: string
          description: "Current journey stage name"
        completedCourses:
          type: integer
        totalCourses:
          type: integer
        completedQuests:
          type: integer
        totalQuests:
          type: integer
        unlockedAchievements:
          type: integer
        totalAchievements:
          type: integer
        recentActivity:
          type: array
          items:
            $ref: "#/components/schemas/ActivityItem"
        upcomingDeadlines:
          type: array
          items:
            $ref: "#/components/schemas/DeadlineItem"
      required: [studentName, currentRole, level, xp, xpToNextLevel, currentStage, completedCourses, totalCourses, completedQuests, totalQuests, unlockedAchievements, totalAchievements, recentActivity, upcomingDeadlines]

    ActivityItem:
      type: object
      properties:
        id:
          type: integer
        type:
          type: string
        description:
          type: string
        xpEarned:
          type: integer
        timestamp:
          type: string
      required: [id, type, description, xpEarned, timestamp]

    DeadlineItem:
      type: object
      properties:
        id:
          type: integer
        title:
          type: string
        dueDate:
          type: string
        courseTitle:
          type: string
        type:
          type: string
      required: [id, title, dueDate, courseTitle, type]

    ProgressMap:
      type: object
      properties:
        stages:
          type: array
          items:
            $ref: "#/components/schemas/ProgressStage"
        currentStageIndex:
          type: integer
      required: [stages, currentStageIndex]

    ProgressStage:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        location:
          type: string
        isCompleted:
          type: boolean
        isCurrent:
          type: boolean
        isLocked:
          type: boolean
        xpRequired:
          type: integer
        imageUrl:
          type: string
      required: [id, name, location, isCompleted, isCurrent, isLocked, xpRequired, imageUrl]

    QuizSummary:
      type: object
      properties:
        id:
          type: integer
        title:
          type: string
        description:
          type: string
        category:
          type: string
          description: "history, geography, culture, nature, ecology"
        difficulty:
          type: string
        questionCount:
          type: integer
        xpReward:
          type: integer
        estimatedMinutes:
          type: integer
        imageUrl:
          type: string
        isCompleted:
          type: boolean
        bestScore:
          type: integer
          nullable: true
      required: [id, title, description, category, difficulty, questionCount, xpReward, estimatedMinutes, imageUrl, isCompleted]

    QuizQuestion:
      type: object
      properties:
        id:
          type: integer
        question:
          type: string
        options:
          type: array
          items:
            type: string
        explanation:
          type: string
      required: [id, question, options, explanation]

    Quiz:
      type: object
      properties:
        id:
          type: integer
        title:
          type: string
        description:
          type: string
        category:
          type: string
        difficulty:
          type: string
        xpReward:
          type: integer
        imageUrl:
          type: string
        questions:
          type: array
          items:
            $ref: "#/components/schemas/QuizQuestion"
      required: [id, title, description, category, difficulty, xpReward, imageUrl, questions]

    QuizSubmission:
      type: object
      properties:
        answers:
          type: array
          items:
            type: integer
          description: "Index of chosen option for each question"
      required: [answers]

    QuizAnswerResult:
      type: object
      properties:
        questionId:
          type: integer
        correct:
          type: boolean
        correctAnswerIndex:
          type: integer
        explanation:
          type: string
      required: [questionId, correct, correctAnswerIndex, explanation]

    QuizResult:
      type: object
      properties:
        score:
          type: integer
        total:
          type: integer
        xpEarned:
          type: integer
        passed:
          type: boolean
        results:
          type: array
          items:
            $ref: "#/components/schemas/QuizAnswerResult"
      required: [score, total, xpEarned, passed, results]

    OkResponse:
      type: object
      properties:
        ok:
          type: boolean
      required: [ok]

    AuthUser:
      type: object
      properties:
        id:
          type: integer
        email:
          type: string
        name:
          type: string
        role:
          type: string
          description: "student or admin"
        studentRole:
          type: string
        bio:
          type: string
          nullable: true
        avatarUrl:
          type: string
          nullable: true
        level:
          type: integer
        xp:
          type: integer
      required: [id, email, name, role, studentRole, level, xp]

    AuthWithToken:
      allOf:
        - $ref: "#/components/schemas/AuthUser"
      properties:
        token:
          type: string
      required: [id, email, name, role, studentRole, level, xp, token]

    MeResponse:
      type: object
      properties:
        user:
          allOf:
            - $ref: "#/components/schemas/AuthUser"
          nullable: true

    RegisterRequest:
      type: object
      properties:
        email:
          type: string
        password:
          type: string
          minLength: 6
        name:
          type: string
          minLength: 2
        studentRole:
          type: string
      required: [email, password, name]

    LoginRequest:
      type: object
      properties:
        email:
          type: string
        password:
          type: string
      required: [email, password]

    UpdateProfileRequest:
      type: object
      properties:
        name:
          type: string
        bio:
          type: string
        avatarUrl:
          type: string
        studentRole:
          type: string

    ProfileActivityItem:
      type: object
      properties:
        id:
          type: integer
        type:
          type: string
        description:
          type: string
        xpEarned:
          type: integer
        createdAt:
          type: string
      required: [id, type, description, xpEarned, createdAt]

    QuizAttemptHistory:
      type: object
      properties:
        id:
          type: integer
        quizId:
          type: integer
        quizTitle:
          type: string
        score:
          type: integer
        total:
          type: integer
        xpEarned:
          type: integer
        passed:
          type: boolean
        createdAt:
          type: string
      required: [id, quizId, quizTitle, score, total, xpEarned, passed, createdAt]

    CompletedCourse:
      type: object
      properties:
        courseId:
          type: integer
        title:
          type: string
        completedModules:
          type: integer
        totalModules:
          type: integer
      required: [courseId, title, completedModules, totalModules]

    UnlockedAchievement:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        description:
          type: string
        iconType:
          type: string
        category:
          type: string
        xpReward:
          type: integer
        unlockedAt:
          type: string
      required: [id, name, description, iconType, category, xpReward, unlockedAt]

    Profile:
      type: object
      properties:
        user:
          $ref: "#/components/schemas/AuthUser"
        nextLevelXp:
          type: integer
        currentLevelXp:
          type: integer
        completedQuests:
          type: integer
        completedQuizzes:
          type: integer
        completedModules:
          type: integer
        unlockedAchievements:
          type: array
          items:
            $ref: "#/components/schemas/UnlockedAchievement"
        recentActivity:
          type: array
          items:
            $ref: "#/components/schemas/ProfileActivityItem"
        quizHistory:
          type: array
          items:
            $ref: "#/components/schemas/QuizAttemptHistory"
        completedCourses:
          type: array
          items:
            $ref: "#/components/schemas/CompletedCourse"
      required:
        [user, nextLevelXp, currentLevelXp, completedQuests, completedQuizzes,
         completedModules, unlockedAchievements, recentActivity, quizHistory, completedCourses]

    PublicUser:
      type: object
      properties:
        id: { type: integer }
        name: { type: string }
        role: { type: string }
        studentRole: { type: string }
        bio: { type: string, nullable: true }
        avatarUrl: { type: string, nullable: true }
        level: { type: integer }
        xp: { type: integer }
      required: [id, name, role, studentRole, level, xp]

    PublicProfile:
      type: object
      properties:
        user:
          $ref: "#/components/schemas/PublicUser"
        completedQuests:
          type: integer
        completedQuizzes:
          type: integer
        completedModules:
          type: integer
        unlockedAchievements:
          type: array
          items:
            $ref: "#/components/schemas/UnlockedAchievement"
      required: [user, completedQuests, completedQuizzes, completedModules, unlockedAchievements]

    AdminUserRow:
      type: object
      properties:
        id:
          type: integer
        email:
          type: string
        name:
          type: string
        role:
          type: string
        studentRole:
          type: string
        level:
          type: integer
        xp:
          type: integer
        createdAt:
          type: string
        completedQuizzes:
          type: integer
        completedQuests:
          type: integer
      required: [id, email, name, role, studentRole, level, xp, createdAt, completedQuizzes, completedQuests]

    AdminStats:
      type: object
      properties:
        totalUsers:
          type: integer
        totalAdmins:
          type: integer
        totalCourses:
          type: integer
        totalQuests:
          type: integer
        totalQuizzes:
          type: integer
        totalCommunityPosts:
          type: integer
        quizAttemptsLast7d:
          type: integer
        moduleCompletionsLast7d:
          type: integer
      required:
        [totalUsers, totalAdmins, totalCourses, totalQuests, totalQuizzes,
         totalCommunityPosts, quizAttemptsLast7d, moduleCompletionsLast7d]

    CreateCourseRequest:
      type: object
      properties:
        title:
          type: string
        description:
          type: string
        role:
          type: string
        stage:
          type: string
        category:
          type: string
        xpReward:
          type: integer
        imageUrl:
          type: string
      required: [title, description, role, stage, category]

    CreateQuestRequest:
      type: object
      properties:
        title:
          type: string
        description:
          type: string
        type:
          type: string
        difficulty:
          type: string
        location:
          type: string
        xpReward:
          type: integer
        timeEstimate:
          type: integer
      required: [title, description, type, difficulty, location]

    LeaderboardEntry:
      type: object
      properties:
        rank:
          type: integer
        studentName:
          type: string
        level:
          type: integer
        xp:
          type: integer
        role:
          type: string
        avatarUrl:
          type: string
        completedQuests:
          type: integer
      required: [rank, studentName, level, xp, role, completedQuests]
